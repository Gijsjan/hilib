.input
	input(type="text", data-view-id!="<%= viewId %>", placeholder!="<%= placeholder %>")
	.caret
<% if (editable) { %>
button.edit Edit
<% } %>
ul.list
ul.selected
	| <% selected.each(function(model) { %>
	li.selected(data-id!="<%= model.id %>") <%= model.get('title') %>
	| <% }); %>
