.overlay
.modalbody
	header
		| <% if (title !== '') { %>
		h2 <%= title %>
		| <% } %>
		<p class="message"></p>
	.body
	<% if (cancelAndSubmit) { %>
	footer
		button.cancel <%= cancelValue %>
		button.submit <%= submitValue %>
	<% } %>